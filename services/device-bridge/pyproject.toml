[project]
name = "botonera2-device-bridge"
version = "0.0.0"
description = "Scaffold importable del futuro bridge de dispositivos"
requires-python = ">=3.14,<3.15"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
